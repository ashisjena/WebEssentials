var str = '12312';

var num = +str;

console.log(num);
console.log(typeof num);

console.log(parseInt(str, 10)); // 10 is to show that it's decimal. It's always good to use the base.

console.log(Number(str));